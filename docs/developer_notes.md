# To develop the docs

First time, install docsify

```
npm i docsify-cli -g
```

Then you can serve the dev in this directory using

```
docsify serve
```

And view the result on

```
http://localhost:3000
```